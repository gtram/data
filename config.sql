CREATE TABLE HOSTS (ID INT PRIMARY KEY AUTOINCREMENT,HOSTNAME TEXT,GROUP INT);
CREATE TABLE GROUPS(ID INT PRIMARY KEY AUTOINCREMENT,NAME TEXT NOT NULL);
CREATE TABLE OPERATIONS(ID INT PRIMARY KEY, NAME TEXT,CREATED_BY TEXT NOT NULL,CREATE_ON DATE NOT NULL,UPDATED_BY TEXT NOT NULL);
CREATE TABLE COMMANDS (ID INT PRIMARY KEY AUTOINCREMENT,COMMAND TEXT NOT NULL);
CREATE TABLE CHANGE (ID INT PRIMARY KEY AUTOINCREMENT, DESCRIPTION TEXT NOT NULL,IMPLEMENTATION TEXT,ROLLBACK TEXT,TESTPLAN ID,SCHEDULED_ON DATE, OPERATION INT,STATUS TEXT,RECORD_STATE INT);
